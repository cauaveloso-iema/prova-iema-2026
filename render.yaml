services:
  - type: web
    name: prova-online
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      # REMOVA esta linha ↓
      # - key: PORT
      #   value: 10000